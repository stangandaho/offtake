% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indices.R
\name{ot_hyco}
\alias{ot_hyco}
\title{Hunting yield comparison (HYCo)}
\usage{
ot_hyco(data, yield, group, reference, effort = NULL, alpha = 0.05)
}
\arguments{
\item{data}{A data frame in \emph{long} format with one row per harvest record.
It must contain at least a yield column (\code{yield}) and a site column
(\code{group}); optionally a hunting-effort column (\code{effort}).}

\item{yield}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of harvested
biomass (e.g. kg) or number of animals taken.}

\item{group}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column identifying
the site / hunting intensity.}

\item{reference}{Character scalar naming the level of \code{group} treated as the
less-hunted reference (e.g. \code{"low"}).}

\item{effort}{<\code{\link[rlang:args_data_masking]{data-masked}}> Optional column of
hunting effort (e.g. hunter-days). When supplied, \code{yield / effort} (CPUE)
is compared instead of raw yield.}

\item{alpha}{Significance level for the one-sided test (default \code{0.05}).}
}
\value{
An \link[=ot_hyco]{offtake} tibble with \strong{one row per hunted site}. The
columns are the same as for \code{\link[=ot_pdc]{ot_pdc()}}, except that \code{hunted_value} and
\code{reference_value} hold the mean yield (or mean CPUE when \code{effort} is
supplied) at each site rather than density; \code{sustainable} is \code{FALSE} when
the more-hunted site yields significantly less.
}
\description{
Compares harvested biomass (or catch-per-unit-effort, CPUE) between more- and
less-hunted sites. Lower yields at the more-hunted site(s) are interpreted as
unsustainable (Adounke et al. 2026; Weinbaum et al. 2013). If an \code{effort}
column is supplied the metric compared is CPUE (\code{yield / effort}); otherwise
raw yield is compared.
}
\examples{
# One row per harvest record. `site` = intensity, `kg` = biomass taken,
# `days` = hunter-days of effort.
d <- data.frame(
  site = rep(c("low", "high"), each = 3),  # less- vs more-hunted
  kg = c(40, 45, 38, 20, 25, 18), # harvested biomass (kg)
  days = c(10, 11, 9, 10, 12, 9) # hunting effort (hunter-days)
)
# Compare CPUE (kg per hunter-day):
ot_hyco(d, yield = kg, group = site, reference = "low", effort = days)
}
\references{
Adounke, G. R. M. et al. (2026) Systematic review of sustainability
assessment approaches for wildlife exploitation. \emph{Biological Conservation}
313, 111606. \doi{10.1016/j.biocon.2025.111606}

Weinbaum, K. Z., Brashares, J. S., Golden, C. D. & Getz, W. M. (2013)
Searching for sustainability: are assessments of wildlife harvests behind
the times? \emph{Ecology Letters} 16, 99-111. \doi{10.1111/ele.12008}
}
\seealso{
\code{\link[=ot_pdc]{ot_pdc()}}, \code{\link[=ot_asc]{ot_asc()}}
}
