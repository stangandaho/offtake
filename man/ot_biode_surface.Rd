% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-biode.R
\name{ot_biode_surface}
\alias{ot_biode_surface}
\title{Extract the predicted density surface from a Biode result}
\usage{
ot_biode_surface(x)
}
\arguments{
\item{x}{An \link[=ot_biode]{offtake} object returned by \code{\link[=ot_biode]{ot_biode()}}.}
}
\value{
A tibble with \strong{one row per grid cell} and the columns:
\describe{
\item{x}{Cell-centre x coordinate (coordinate unit of the settlements).}
\item{y}{Cell-centre y coordinate.}
\item{density}{Predicted steady-state game density in that cell.}
}
This long format is ready for mapping, e.g. with
\code{ggplot2::geom_raster(ggplot2::aes(x, y, fill = density))}.
}
\description{
Extract the predicted density surface from a Biode result
}
\examples{
settlements <- data.frame(xkm = 10, ykm = 10, hunters = 100)
res <- ot_biode(settlements, x = xkm, y = ykm, humans = hunters,
                k = 25, r = 0.07, hphy = 40, kill_rate = 0.1, sigma = 6,
                resolution = 2)
ot_biode_surface(res)
}
\seealso{
\code{\link[=ot_biode]{ot_biode()}}
}
