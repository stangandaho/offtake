% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-production.R
\name{ot_pro}
\alias{ot_pro}
\title{Robinson & Redford production model (Pro)}
\usage{
ot_pro(
  data,
  k,
  harvest,
  lambda = NULL,
  f = NULL,
  longevity = NULL,
  b = NULL,
  a = NULL,
  w = NULL
)
}
\arguments{
\item{data}{A data frame with \strong{one row per species or population unit}.
Required columns: carrying capacity (\code{k}) and observed annual offtake
(\code{harvest}), on the \emph{same basis} (both per km^2, or both absolute counts).
You must also supply the growth rate -- either a \code{lambda} column or the
life-history columns \code{b}, \code{a}, \code{w} -- and the mortality factor -- either an
\code{f} column or a \code{longevity} column.}

\item{k}{<\code{\link[rlang:args_data_masking]{data-masked}}> Carrying capacity \code{K}
(density per km^2, or absolute population size).}

\item{harvest}{<\code{\link[rlang:args_data_masking]{data-masked}}> Observed annual
offtake, on the same basis as \code{k}.}

\item{lambda}{<\code{\link[rlang:args_data_masking]{data-masked}}> Maximum finite rate
of increase \eqn{\lambda_{max}} (dimensionless, > 1). Optional if \code{b}, \code{a},
\code{w} are given.}

\item{f}{<\code{\link[rlang:args_data_masking]{data-masked}}> Mortality factor \code{F}
(0.2, 0.4 or 0.6). Optional if \code{longevity} is given.}

\item{longevity}{<\code{\link[rlang:args_data_masking]{data-masked}}> Age at last
reproduction / lifespan (years), used to derive \code{F} when \code{f} is not given.}

\item{b, a, w}{<\code{\link[rlang:args_data_masking]{data-masked}}> Life-history columns
passed to \code{\link[=ot_lambda_max]{ot_lambda_max()}} when \code{lambda} is not given: annual female
offspring per female (\code{b}), age at first reproduction (\code{a}) and age at last
reproduction (\code{w}), all in years.}
}
\value{
An \link[=ot_pro]{offtake} tibble with \strong{one row per input row} and the
columns:
\describe{
\item{lambda_max}{Maximum finite rate of increase used, whether supplied or
computed from \code{b}, \code{a}, \code{w}.}
\item{f}{Mortality factor \code{F} used (0.2, 0.4 or 0.6).}
\item{production}{Estimated maximum sustainable harvest \code{P}, on the same
basis as \code{k} and \code{harvest} (e.g. individuals per km^2 per year).}
\item{harvest}{The observed annual offtake, echoed back for comparison.}
\item{sustainable}{Logical verdict: \code{TRUE} when \code{harvest <= production},
\code{FALSE} when the observed offtake exceeds the estimated production.}
}
}
\description{
Estimates the maximum sustainable harvest (production) of a population from
its carrying capacity, maximum rate of increase and a longevity-based
mortality factor, and compares it with the observed annual offtake. This is
the most widely used model in bushmeat sustainability assessments
(Adounke et al. 2026).
}
\details{
The production (maximum sustainable number that can be taken per year) is
\deqn{P = 0.6\,K\,(\lambda_{max} - 1)\,F}
where \code{K} is carrying-capacity density (or population size), \eqn{\lambda_{max}}
is the maximum finite rate of increase and \code{F} is the mortality factor set by
longevity: \code{F = 0.6} for short-lived species (lifespan ~ 5 years), \code{0.4} for
medium-lived (5-10 years) and \code{0.2} for long-lived species (> 10 years)
(Robinson & Redford 1991). The factor \verb{0.6 K} is the density at which
production is assumed maximal. \strong{If the observed harvest exceeds \code{P} the
harvest is considered unsustainable.}

Supply \eqn{\lambda_{max}} through \code{lambda} directly, or leave it \code{NULL} and
provide the life-history columns \code{b}, \code{a}, \code{w} so it is computed with
\code{\link[=ot_lambda_max]{ot_lambda_max()}} (Cole's equation). Provide the mortality factor either
through \code{f} directly or through a \code{longevity} column.
}
\examples{
# One row per species. Columns:
#   dens = carrying-capacity density (ind / km^2)
#   offtake = observed annual harvest (ind / km^2 / year)
#   lifespan = age at last reproduction (years) -> sets F
#   lam = maximum finite rate of increase (lambda_max)
d <- data.frame(
  species = c("red duiker", "blue duiker"),
  dens = c(10, 25),
  offtake = c(3.0, 6.0),
  lifespan = c(9, 6),
  lam = c(1.35, 1.55)
)
ot_pro(d, k = dens, harvest = offtake, lambda = lam, longevity = lifespan)
}
\references{
Robinson, J. G. & Redford, K. H. (1991) Sustainable harvest of Neotropical
forest mammals. In \emph{Neotropical Wildlife Use and Conservation} (eds Robinson
& Redford), 415-429. University of Chicago Press.

Adounke, G. R. M. et al. (2026) Systematic review of sustainability
assessment approaches for wildlife exploitation. \emph{Biological Conservation}
313, 111606. \doi{10.1016/j.biocon.2025.111606}
}
\seealso{
\code{\link[=ot_pbr]{ot_pbr()}}, \code{\link[=ot_msy]{ot_msy()}}, \code{\link[=ot_lambda_max]{ot_lambda_max()}}
}
