% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ot_lambda_max}
\alias{ot_lambda_max}
\title{Maximum finite rate of population increase (Cole's equation)}
\usage{
ot_lambda_max(b, a, w, interval = c(1e-06, 5))
}
\arguments{
\item{b}{Mean annual number of female offspring per female (i.e.
litter size \eqn{\times} litters per year \eqn{\times} proportion female).}

\item{a}{Age at first reproduction (years).}

\item{w}{Age at last reproduction (years); often approximated by lifespan.}

\item{interval}{Search interval for \eqn{r_{max}} passed to
\code{\link[stats:uniroot]{stats::uniroot()}}.}
}
\value{
A numeric vector, recycled to the length of the longest of \code{b}, \code{a}
and \code{w}, giving \eqn{\lambda_{max}} -- the maximum finite (per-year) rate
of increase. It is dimensionless and greater than 1 for a growing
population (e.g. \code{1.30} means the population can grow by at most 30\% per
year). \code{NA} is returned for elements with missing inputs or with no
positive root inside \code{interval}.
}
\description{
Solves Cole's (1954) characteristic equation for the maximum intrinsic rate
of natural increase, returning the finite (per-year) multiplication rate
\eqn{\lambda_{max} = e^{r_{max}}}. This is the rate required by the
production model \code{\link[=ot_pro]{ot_pro()}} when it is not supplied directly.
}
\details{
Cole's equation is
\deqn{1 = e^{-r} + b\,e^{-r a} - b\,e^{-r (w + 1)}}
where \code{b} is the mean annual number of \emph{female} offspring per female, \code{a}
is the age at first reproduction and \code{w} is the age at last reproduction
(all in years). The equation is solved numerically for \eqn{r} and
\eqn{\lambda_{max}=e^{r}} is returned.
}
\examples{
# Inputs are life-history traits (not columns of a data frame here):
#   b = female young per female per year, a = age at first reproduction,
#   w = age at last reproduction. A duiker breeding from age 1 to 8:
ot_lambda_max(b = 0.6, a = 1, w = 8)

# Vectorised over several species at once:
ot_lambda_max(b = c(0.6, 0.5), a = c(1, 2), w = c(8, 12))
}
\references{
Cole, L. C. (1954) The population consequences of life history phenomena.
\emph{The Quarterly Review of Biology} 29, 103-137.

Robinson, J. G. & Redford, K. H. (1986) Intrinsic rate of natural increase
in Neotropical forest mammals. \emph{Oecologia} 68, 516-520.
}
