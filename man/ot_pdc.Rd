% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indices.R
\name{ot_pdc}
\alias{ot_pdc}
\title{Population density comparison (PDC)}
\usage{
ot_pdc(data, density, group, reference, alpha = 0.05)
}
\arguments{
\item{data}{A data frame in \emph{long} format with one row per density estimate.
It must contain at least: a numeric density column (\code{density}) and a
site/treatment column (\code{group}) whose values include the reference level
and one or more hunted levels. Extra columns are ignored.}

\item{density}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of
abundance or density estimates (e.g. individuals per km^2). Higher = more
animals.}

\item{group}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column identifying
the site or hunting treatment. Must contain the \code{reference} level and at
least one hunted level.}

\item{reference}{Character scalar naming the level of \code{group} treated as the
unhunted / lightly hunted reference (e.g. \code{"reference"}).}

\item{alpha}{Significance level for the one-sided test (default \code{0.05}).}
}
\value{
An \link[=ot_pdc]{offtake} tibble with \strong{one row per hunted site} and the
columns:
\describe{
\item{hunted_site}{The hunted level of \code{group} being assessed.}
\item{reference_site}{The reference level it is compared against.}
\item{hunted_value}{Mean density at the hunted site.}
\item{reference_value}{Mean density at the reference site.}
\item{pct_change}{Percentage change of the hunted site relative to the
reference, \code{(hunted - reference) / reference * 100}. Negative means the
hunted site is depleted.}
\item{p_value}{P-value of the one-sided Welch \emph{t}-test that the hunted
site has a \emph{lower} mean than the reference. \code{NA} when a site has fewer
than two replicates.}
\item{sustainable}{Logical verdict. \code{FALSE} when the hunted site is
significantly lower than the reference (\code{p_value < alpha}); otherwise
\code{TRUE}.}
}
}
\description{
Compares wildlife abundance/density between hunted and reference
(unhunted or lightly hunted) sites. Lower density at the hunted site(s) is
interpreted as unsustainable (Adounke et al. 2026; Weinbaum et al. 2013).
}
\details{
Provide \strong{one row per density estimate} (e.g. per line transect or
camera-trap station). With two or more replicates per site a one-sided Welch
\emph{t}-test is used (hunted < reference); with a single value per site the
verdict falls back to the sign of the difference and a warning is issued.
}
\examples{
# One row per transect. `site` = treatment, `dens` = animals per km^2.
d <- data.frame(
  site = rep(c("control", "hunted"), each = 4), # reference vs hunted
  dens = c(12, 14, 11, 13, 6, 7, 5, 8) # density per transect
)
ot_pdc(d, density = dens, group = site, reference = "control")
}
\references{
Adounke, G. R. M. et al. (2026) Systematic review of sustainability
assessment approaches for wildlife exploitation. \emph{Biological Conservation}
313, 111606. \doi{10.1016/j.biocon.2025.111606}

Weinbaum, K. Z., Brashares, J. S., Golden, C. D. & Getz, W. M. (2013)
Searching for sustainability: are assessments of wildlife harvests behind
the times? \emph{Ecology Letters} 16, 99-111. \doi{10.1111/ele.12008}
}
\seealso{
\code{\link[=ot_hyco]{ot_hyco()}}, \code{\link[=ot_asc]{ot_asc()}}
}
